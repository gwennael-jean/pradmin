version: '3.8'

services:
  php:
    build:
      context: ./services/php
      args:
        USER_ID: ${USER_ID:-0}
        GROUP_ID: ${GROUP_ID:-0}
    ports:
      - ${PHP_PORT}:8000
    volumes:
      - ${SOURCE}:/var/www

  node:
    image: bitnami/node
    volumes:
      - ${SOURCE}:/app
    command: ["npm", "run", "watch"]

  redis:
    image: redis
    command: --loglevel verbose